// Stub. Implement loading of seed/business_units.csv, seed/emission_factors.csv,
// and seed/activities.csv into your schema. Decide what to do with the deliberately
// problematic rows in activities.csv (reject? quarantine? log?) — that's part of the
// assessment.

console.log("seed: not implemented yet — wire this up to your schema");
process.exit(0);
